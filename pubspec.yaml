name: voicecrm
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter

  url_launcher: ^6.3.0
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  http: ^1.2.0

  # Call handling
  phone_state: ^1.0.4
  call_log: ^6.0.0

  # Recording (Android-safe)
  record: ^6.1.1

  # UI
  google_fonts: ^6.3.1
  go_router: ^16.2.1
  flutter_riverpod: ^2.6.1
  audioplayers: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true